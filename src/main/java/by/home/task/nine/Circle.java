package by.home.task.nine;
import static java.lang.Math.*;

public class Circle {

    public static double checkCircumference(double radius){
        return (2*PI*radius);
    }

    public static double checkAreaOfCircle(double radius){
        return PI*pow(radius,2);
    }
}
